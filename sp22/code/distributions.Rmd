---
title: "distributions"
author: "Arielle Herman"
date: "5/25/2022"
output: pdf_document
---

```{r setup, include=FALSE}
source("default_setup.R")
```

# income distribution

# employment

# households

```{r}
wrangled %>% ggplot(aes(x = hh_size)) + geom_histogram()
wrangled %>% filter(age >= 65) %>% filter(hh_sn_65_bi == 0) %>% select(age, starts_with("hh"))
wrangled %>% mutate(age_cat = case_when(
  age >= 65 ~ "65+",
  age >= 18 ~ "18-64"
)) %>% count(age_cat, hh_sn_65_bi, hh_ad_18_64 > 0)

wrangled %>% filter(age >= 18, age < 65, hh_ad_18_64 < 1) %>% select(age, hh_ad_18_64)
#final_clean %>% filter(q5 >= 18, q5 < 65, q26_2 < 1) %>% count(q5, starts_with("26"))
final_clean %>% filter(is.na(test_response), valid == 1, q5 >= 18, q5 < 65, q26_2 < 1) %>% select(responseid, q5, q26_1, q26_2, q26_3, q26_4)

wrangled %>% filter(responseid == "r_3h3hbvcijpvzgmm")
  
  
wrangled %>% ggplot(aes(x = age)) + geom_histogram()
wrangled %>% ggplot(aes(x = duration)) + geom_histogram()
```